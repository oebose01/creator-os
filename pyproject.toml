[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q --strict-markers --cov=core --cov-report=term-missing"
testpaths = ["tests"]

[tool.coverage.run]
source = ["core"]
branch = true

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "def __repr__", "if self.debug:"]
